# 05 Update wiki tool template

Plan
1. Update `docs/wiki/templates/tool.md` to require `name`, `domain`, `summary`, `schema`, `example_request`, `example_response`, `status`, `live_versions`, `owners`.
2. Document template in `CONTRIBUTING.md`.

Verification
- New pages validate against template.

Commit message
- docs: require YAML frontmatter fields in wiki tool template
