# 03 Implement `validate_wiki_parity.py`

Plan
1. Implement `scripts/validate_wiki_parity.py` to verify registry == implemented tools and wiki coverage.
2. Emit JSON report and human summary; exit non-zero on errors.

Verification
- Validator fails if mismatch detected.

Commit message
- feat(docs): add validator for registry↔implementations↔wiki parity
