# Phase 5 — Makefile targets and local gate

Plan
1. Add Makefile targets: `generate-from-wiki`, `validate-wiki`, `validate-docstrings`, and chain them in `make all`.

Verification
- Local `make all` runs full docs validator.

Commit message
- chore(make): add docs generator and validation targets
