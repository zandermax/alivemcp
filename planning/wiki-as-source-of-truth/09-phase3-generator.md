# Phase 3 — Generator implementation

Plan
1. Implement full `generate_from_wiki.py` features: code parsing, frontmatter merge, chunked output.
2. Keep `--check` default; `--apply` writes outputs to specified dirs.

Verification
- Generated `mcp_tool_defs/part_*.json` + `index.json` match expectations.

Commit message
- feat(docs): implement generator merge and chunked output
