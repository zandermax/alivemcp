# Phase 4 — Parity & docstring verification

Plan
1. Wire `scripts/validate_wiki_parity.py` and `docstring_checker.py` into `make validate-wiki`.

Verification
- `make validate-wiki` fails on mismatch; JSON report produced.

Commit message
- test(docs): add parity and docstring verification to Makefile
